# Dependencies (reinstall with `npm install`)
node_modules/

# Build output
dist/
out/
build/
# …but keep Store/appx source tile assets (not build output)
!build/
!build/appx/
!build/appx/**

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local env / secrets (license keys, signing certs, etc. — NEVER commit)
.env
.env.*
*.pem
*.p12
*.pfx
csc-*.json

# Local website test models (not committed — large/3rd-party)
website/models/
